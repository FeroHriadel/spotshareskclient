import Nav from '../components/Nav';
import { render, screen, fireEvent } from '@testing-library/react';
//import { something } from 'react-router;



test('Nav subitems should be hidden initially', () => {
    render (<Nav />);
});

test('Nav subitems show when main-item is hovered & hide when unhovered', () => {

});

test('Clicking a subitem navigates to a corresponding page', () => {
    
});

test('login & register is removed when user is logged-in', () => {

});

test('log out shows when user is signed-in', () => {

});

test('logout function clears user from Context', () => {

});

