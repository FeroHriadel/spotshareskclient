
test('onSubmit returns message if bad email or password', () => {

});

test('redirects away if user is logged-in', () => {

});

test('onSubmit dispatches email & token to Context', () => {

});

test('onSubmit runs userCreate mutation and shows update/onError message', () => {

});

test('googleLogin dispatches email & token to Context', () => {

});

test('googleLogin runs userCreate mutation and shows update/onError message', () => {

});