




test('redirects away when signed-in user tries to navigate to /register', () => {

});

test('renders message when no email or short password', () => {

});

test('renders message on submit', () => {

});